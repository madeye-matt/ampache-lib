(ns com.madeye.clojure.ampache.ampachedb-test
  (:require [clojure.test :refer :all]
            [com.madeye.clojure.ampache.ampachedb :refer :all]))

(deftest a-test
  (testing "FIXME, I fail."
    (is (= 0 1))))
